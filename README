We analyse US gun murder data collected bi the FBI

download-data.R - downloads csv files to data directory

murders.R - creates a derived dataset and saves as R object in rda directory

analysis.R - generates a plot and saves it to the figs directory